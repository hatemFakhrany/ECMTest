Office.onReady(() => {
  // This file is loaded by commands.html.
  // Your manifest currently uses ShowTaskpane actions, which don't need
  // custom JS functions here. If you later add ExecuteFunction buttons,
  // register their handlers with Office.actions.associate("functionName", handlerFn)
  // and Office.actions.associate again for each additional button.
});
